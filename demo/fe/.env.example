# Demo FE — copy to .env.local if needed.
#
# Browser calls same-origin `/api` and `/health`; Vite proxies to the BE below.
# Default: http://127.0.0.1:8000 (BE on the same machine as `npm run dev`).
#
# VITE_PROXY_BE_TARGET=http://127.0.0.1:8000
